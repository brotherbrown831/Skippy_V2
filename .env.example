# OpenAI (required)
OPENAI_API_KEY=sk-your-key-here

# Database (matches docker-compose postgres service)
DATABASE_URL=postgresql://skippy:skippy@postgres:5432/skippy

# General
# TIMEZONE=America/Chicago

# LLM model overrides
# LLM_MODEL=gpt-4o-mini
# EMBEDDING_MODEL=text-embedding-3-small

# Home Assistant
HA_URL=http://homeassistant.local:8123
HA_TOKEN=your-long-lived-access-token
# HA_NOTIFY_SERVICE=mobile_app_your_device

# Twilio SMS (optional)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=
# TWILIO_TO_NUMBER=

# Telegram (optional)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_ALLOWED_CHAT_IDS=
# TELEGRAM_NOTIFY_CHAT_IDS=
# TELEGRAM_POLL_INTERVAL=2
# TELEGRAM_LONG_POLL_TIMEOUT=20
# TELEGRAM_API_BASE=https://api.telegram.org

# Google Calendar (optional, requires service account)
# GOOGLE_CALENDAR_ID=your-email@gmail.com
# GOOGLE_SERVICE_ACCOUNT_JSON=credentials/google-sa.json

# Google OAuth2 — Gmail + Contacts (optional, run scripts/google_oauth.py first)
# GOOGLE_OAUTH_CLIENT_JSON=credentials/google-oauth.json
# GOOGLE_OAUTH_TOKEN_JSON=credentials/google-token.json

# Scheduler
# SCHEDULER_ENABLED=true
# CALENDAR_CHECK_INTERVAL_MINUTES=30

# Scheduled Job Times (24-hour format HH:MM, or "disabled")
# All times use the TIMEZONE setting (default: America/Chicago)
# Set to "disabled" to prevent a job from running
# Leave commented to use defaults shown below

# Morning briefing with calendar and tasks (default: 07:00 / 7 AM)
# MORNING_BRIEFING_TIME=07:00

# Evening summary for tomorrow (default: 22:00 / 10 PM)
# EVENING_SUMMARY_TIME=22:00

# Google Contacts sync (default: 02:00 / 2 AM)
# GOOGLE_CONTACTS_SYNC_TIME=02:00

# People importance score recalculation (default: 03:00 / 3 AM)
# PEOPLE_IMPORTANCE_RECALC_TIME=03:00

# Examples:
# MORNING_BRIEFING_TIME=06:30        # Run at 6:30 AM
# EVENING_SUMMARY_TIME=disabled      # Disable evening summaries
# GOOGLE_CONTACTS_SYNC_TIME=01:00    # Sync at 1 AM instead

# Response limits
# VOICE_MAX_TOKENS=300
# CHAT_MAX_TOKENS=4096

# Memory tuning
# MEMORY_SIMILARITY_THRESHOLD=0.15
# MEMORY_RETRIEVAL_LIMIT=5
# MEMORY_DEDUP_THRESHOLD=0.8

# Tavily Web Search (optional)
# Get API key from https://tavily.com
# TAVILY_API_KEY=tvly-your-api-key-here

# WhatsApp Adapter (optional)
# First run: docker compose logs -f whatsapp_adapter (scan QR code shown)
# Sessions are persisted in whatsapp_sessions Docker volume automatically.
# SKIPPY_BASE_URL=http://skippy:8000
# WHATSAPP_SESSION_PATH=/app/sessions
# WHATSAPP_ALLOWED_NUMBERS=15551234567,15559876543
# WHATSAPP_ALLOW_GROUPS=false
# SKIPPY_TIMEOUT_MS=30000
